package com.victor.reconloop;

/** The operator-selected class of a run, retained with every outbound decision. */
enum ScanProfile {
    PASSIVE,
    DISCOVERY,
    ACTIVE_SAFE,
    ACTIVE_MUTATION,
    EXTERNAL_TOOL
}
